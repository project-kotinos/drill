build_targets:
- commands:
  - mvn install
  container:
    image: yourbase/yb_ubuntu:18.04
  name: default

ci:
  builds:
  - build_target: default
    name: default
dependencies:
  build:
  - java:14+36
  - maven:3.6.3
